# TASKS.md — DancingHippo 改造任务清单

> 按阶段从上往下做。**每阶段必须先达成「完成标准」、并让主人跑完「请主人验证」，才进下一阶段。**
> 做完一项就把 `[ ]` 改成 `[x]`。

---

## 阶段 0：跑通原版基线（一行代码都先别改）

目的：确认未改动的原版在这台机器上能出片。基线不通，后面全是空谈。

- [x] 当前仓库已有 Git 历史；已切到工作分支 `codex/dancinghippo-phase0-baseline`，未创建 commit。
- [ ] 按 README 装好：`setup.bat` 装依赖；确认 ComfyUI 装了 `ComfyUI-LTXVideo` 和 `ComfyUI-VideoHelperSuite` 两个节点包；LTX-2.3 视频模型、两个 VAE、文本编码器、关键帧图像模型都放到位。
- [x] 已用 LAN ComfyUI `192.168.1.63:8188` 验证关键帧和 LTX 视频 API 工作流能出东西并下载回本地。
- [x] 已用 LAN Ollama `192.168.1.225:11434` 验证 `gemma4:12b` 能返回可解析 JSON。
- [x] 已跑通 1 分钟基线项目 `phase0_baseline_remote_ltx`，生成 `video_director_agent/output/phase0_baseline_remote_ltx/final.mp4`。

**完成标准**：用一句英文提示能出一条 1 分钟的成片 `final.mp4`。

**请主人验证**：已验证 `final.mp4` 能播放，声音效果通过。

---

## 阶段 1：中文化（换 Qwen）

目的：把 LLM 从 Gemma 换成中文更好的 Qwen，让剧本/对白是地道中文。

- [x] 已在 LAN Ollama `192.168.1.225:11434` 确认可用 Qwen 标签：`qwen3.5:9b`。
- [x] 已在 `user_settings.json` 把 `ollama_model_fast` / `ollama_model_creative` 切到 `qwen3.5:9b`。
- [x] 已跑多次 `breakdown()` 验证 Qwen 返回 JSON 可解析；截断或格式异常时，现有修复/重试路径能恢复。
- [x] 已增加可配置输出语言 `output_language`，当前设为 `simplified_chinese`，场景描述和对白输出为简体中文。
- [x] 已补充中文时间单位识别和中文对白时长估算，避免 `20秒` / 中文旁白被当成极短内容。

**完成标准**：同一句中文主意能产出中文场景描述、中文对白，且 JSON 不崩。

**请主人验证**：「现在用中文输入一句主意，看拆出来的场景描述和对白是不是通顺的中文，截图发我。」

---

## 阶段 2：配乐开关

目的：默认屏蔽了背景音乐（约束 #5），给主人一个能要配乐的选项。

- [ ] 把 `config.NEGATIVE_PROMPT` 里的 `background music, soundtrack, musical score` 做成**可开关**（比如一个 `ALLOW_MUSIC` 设置：开则去掉这几个词，关则保留），而不是直接删死。
- [ ] 默认值跟主人确认要开还是关。

**完成标准**：一个开关能控制成片有没有背景音乐。

**请主人验证**：「我加了配乐开关。你各出一条短片对比一下，有没有音乐的差别对不对。」

---

## 阶段 3：关键帧图像模型换 Flux2

目的：把出关键帧的模型从默认 Z-Image Turbo 换成主人熟悉的 Flux2。**阶段 0–2 全稳了再做这步**，免得出问题不好定位。

- [ ] 在 ComfyUI 里做一个 Flux2 出图工作流并验证能跑。
- [ ] 按 README 的导出命令重新导出 `keyframe_template.json` 覆盖。
- [ ] 验证约束 #4：关键帧阶段能正常出图，节点自动识别没识别错。

**完成标准**：关键帧由 Flux2 出图，整条流程不受影响。

**请主人验证**：「关键帧换成 Flux2 了，你跑一次看预览图风格对不对、生成顺不顺。」

---

## 阶段 4：小说 → 剧本（前置步骤）

目的：原版只支持「提示→剧本」和「已有剧本→拆解」。加一个把整部小说先转成剧本格式的前置步骤。

- [ ] 新增一个**独立模块/步骤**：输入小说文本 → 用 LLM 产出剧本格式文本（带场景标题 `INT./EXT.`、角色名、对白），让现有的 `_is_script()`/`parse_script()` 能识别。
- [ ] **不破坏现有入口**（约束 #1 的工作方式）：原来的「提示」和「剧本」两种输入照常能用，小说是新增的第三种。
- [ ] 长小说要考虑分段处理，别一次喂爆上下文。

**完成标准**：丢进一段小说，能先转成剧本、再走完原流程出片。

**请主人验证**：「贴一段小说进去，看它先转出来的剧本对不对，再让它继续出片。」

---

## 阶段 5（以后再说）：Wan2.2 作为可选视频后端

目的：在保留 LTX-2.3 为默认的前提下，把 Wan2.2 FLF2V（首帧+尾帧）做成可切换的另一个视频后端。**改动较大，等前面都稳定、主人明确要做时再启动。**

- [ ] 先和主人确认要不要做（Wan2.2 没原生音频，要另接音频，是个权衡）。
- [ ] 设计成可切换的「视频后端」抽象层，LTX 仍是默认。
- [ ] 关键帧阶段改为每镜出**首帧+尾帧两张**，喂给 Wan2.2 FLF2V 工作流。
- [ ] 音频另接（TTS + 音效 + 可选配乐）并做时间对齐。

**完成标准**：界面能选 LTX 或 Wan2.2 两种视频引擎，都能出片。

**请主人验证**：到那一步再定。
